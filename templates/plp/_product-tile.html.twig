{% if product %}
	{% set name = product.abstractName %}
	{% set price = product.prices[0].DEFAULT|default(0) %}
	{% set imgSmall = product.images[0].externalUrlSmall|default('') %}
	{% set imgBig = product.images[0].externalUrlLarge|default('') %}
	{% set link = '#' %}
	<div class="p-2 hover:bg-slate-100">

		<a class="flex" href="{{ link }}">
			<img class="w-full h-48 object-contain" src="{{ imgSmall }}" alt="{{ name }}">
		</a>

		<a class="flex flex-col" href="{{ link }}">
			<span>{{ name }}</span>
			{% if price %}
				<span>€{{ price / 100 }}</span>
			{% endif %}
		</a>
	</div>
{% endif %}
